{
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.6",
    "tailwindcss": "^4.1.11"
  },
  "dependencies": {
    "axios": "^1.10.0"
  },
  "scripts": {
    "start:server": "cd server && npm start",
    "start:client": "cd client-final && npm start",
    "dev": "concurrently \"npm run start:server\" \"npm run start:client\"",
    "build": "cd client-final && npm run build",
    "install:all": "npm install && cd server && npm install && cd ../client-final && npm install"
  }
}
